server:
  port: 8001
  servlet:
    context-path: /provider

spring:
  application:
    name: cloud-payment-service
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://39.96.41.87:3306/db_cloud_study?useSSL=false&useUnicode=true&characterEncoding=utf-8
    username: root
    password: xuejinqingshan1.
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.howe.dto
  global-config:
    db-config:
      table-prefix: tb_
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

eureka:
  client:
    service-url:
      #      defaultZone: http://localhost:7001/eureka
      defaultZone: http://eureka7001.com:7001/eureka/,http://eureka7002.com:7002/eureka/
    fetch-registry: true #  是否从eurekaService抓取已有的注册信息，默认为true，单节点无所谓，集群必须配置为true，配合ribbon使用负载均衡
    register-with-eureka: true # 是否将自己注册计入eurekaService
  instance:
    instance-id: payment8001
    prefer-ip-address: true
    ip-address: 192.168.101.24
